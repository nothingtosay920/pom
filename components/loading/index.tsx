import { Spin } from "@arco-design/web-react"
import { useSelector } from "react-redux"
import { ReduxState } from "../../store/features/status"

const Loding = () => {
  // const value = useSelector<ReduxState, boolean>((state) => state.loadingStatus)
  

  return (
    // <div  style={{'position': 'absolute', 'left': '0' , 'top': '0', 'width': '100vw', 'height': '100vh', 'backgroundColor': 'white', 'display': value ? 'flex' : 'none', 'alignItems': 'center', 'justifyContent': 'center'}}>
      // <Spin size={40} />
    // </div>
    <div></div>
  )
}

export default Loding